<resources>
    <!-- Base application theme. -->
    <style name="Theme.SurfBrowser" parent="Theme.AppCompat.Light">
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="colorAccent">@color/black</item> <!-- 用于强调的颜色 -->
    </style>
</resources>